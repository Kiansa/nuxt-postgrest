<!-- Link an issue if one exists: Fixes #123 -->

## What changed

## How to test

- [ ] `pnpm test` passes locally (needs `pnpm db:up` first)
- [ ] `pnpm test:types` passes
- [ ] Docs updated if behavior or config changed
