---
title: Local PostgREST
description: Run Postgres and PostgREST locally with Docker Compose.
navigation:
  icon: i-simple-icons-docker
---

If you don't have PostgREST running yet, this Compose file gives you Postgres and PostgREST with JWT auth in one command. It is the same setup the module's own tests use.

```yaml [docker-compose.yml]
services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_PASSWORD: postgres
    ports: ['5432:5432']
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro

  postgrest:
    image: postgrest/postgrest:v16.3
    depends_on: [db]
    environment:
      PGRST_DB_URI: postgres://authenticator:authenticator@db:5432/postgres
      PGRST_DB_SCHEMAS: public
      PGRST_DB_ANON_ROLE: anon
      PGRST_JWT_SECRET: replace-with-a-secret-of-at-least-32-characters
    ports: ['3001:3000']
```

The roles referenced above need to exist. A minimal `db/init.sql`:

```sql [db/init.sql]
create role anon nologin;
create role authenticated nologin;
create role service_role nologin bypassrls;
create role authenticator noinherit login password 'authenticator';
grant anon, authenticated, service_role to authenticator;
grant usage on schema public to anon, authenticated, service_role;
```

Every JWT you send must be signed with `PGRST_JWT_SECRET` and carry a `role` claim naming one of these roles. See [Authentication](/guide/authentication) for how tokens reach PostgREST.

::tip
The module repository ships a complete version with seeded tables and RLS policies in `docker-compose.yml` and `db/seed.sql`. It's a good base for bug reproductions.
::
